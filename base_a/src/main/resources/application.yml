server.port: 8090
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/base_a
    username: postgres
    password: example
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update